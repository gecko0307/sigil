// Sigil server URL when running the game locally
var sigilLocalServer = "ws://localhost:8050";

// Sigil server URL when running the game online
var sigilProductionServer = "wss://sigil.pixperfect.online/sigil-server/";
